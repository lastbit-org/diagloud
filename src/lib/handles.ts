/** @deprecated Use `dynamicHandles.ts`. Mantido para compatibilidade de import. */
export {
  DEFAULT_SOURCE_HANDLE,
  DEFAULT_TARGET_HANDLE,
  DEFAULT_EGRESS_HANDLE,
  DEFAULT_INGRESS_HANDLE,
  HANDLE_SIDES,
  makeHandleId,
  parseHandleId,
  normalizeHandleId,
  isValidHandleId,
  matchesHandleRoles,
} from "./dynamicHandles";
