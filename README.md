# Diagloud

Editor visual de arquitetura GCP (MVP: VPC, sub-rede, VM).

## Desenvolvimento

```bash
npm install
npm run dev
```

## Estrutura do código

A convenção de pastas (`types`, `model`, `components/canvas`, `nodes`, `palette`) está documentada em:

**[docs/estrutura-pastas.md](./docs/estrutura-pastas.md)**

## Regras de conexão (VPC, sub-rede, VM)

Matriz e cardinalidade do MVP:

**[docs/regras-conexao.md](./docs/regras-conexao.md)**

## CIDR e IPs de VM

**[docs/cidr-e-ips.md](./docs/cidr-e-ips.md)**
